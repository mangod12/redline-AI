# ---- Core API -----------------------------------------------------------
fastapi>=0.104.1,<1.0
uvicorn[standard]>=0.24.0,<1.0
gunicorn>=21.2.0,<23.0

# ---- Task queue ---------------------------------------------------------
celery>=5.3.6,<6.0
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0
email-validator>=2.1.0,<3.0
sqlalchemy[asyncio]>=2.0.23,<3.0
asyncpg>=0.29.0,<1.0
aiosqlite>=0.20.0,<1.0
alembic>=1.13.0,<2.0

# ---- Cache / Pub-Sub ----------------------------------------------------
redis>=5.0.1,<6.0
fakeredis>=2.20.0                  # Local dev fallback (no real Redis needed)

# ---- Auth ---------------------------------------------------------------
PyJWT[crypto]>=2.8.0,<3.0
filelock>=3.12.0,<4.0
passlib[bcrypt]>=1.7.4,<2.0
python-multipart>=0.0.6,<1.0

# ---- ML / ONNX ----------------------------------------------------------
onnxruntime>=1.17.0,<2.0           # CPU inference; swap for onnxruntime-gpu on GPU nodes
numpy>=1.26.0,<2.0
transformers>=4.36.0,<5.0
optimum[onnxruntime]>=1.16.0,<2.0
tokenizers>=0.15.0,<1.0

# ---- STT (local Whisper — no paid APIs) ----------------------------------
openai-whisper>=20231117
ffmpeg-python>=0.2.0,<1.0

# ---- Observability ------------------------------------------------------
structlog>=24.1.0,<25.0
prometheus-client>=0.20.0,<1.0
starlette-prometheus>=0.9.0,<1.0

# ---- Resilience ---------------------------------------------------------
pybreaker>=1.2.0,<2.0

# ---- Security -----------------------------------------------------------
slowapi>=0.1.9,<1.0
twilio>=9.0.0,<10.0

# ---- Dashboard ----------------------------------------------------------
jinja2>=3.1.0,<4.0
aiofiles>=23.0.0,<24.0

# ---- Cloud (GCS model download for Cloud Run) ----------------------------
google-cloud-storage>=2.14.0,<3.0

# ---- HTTP clients -------------------------------------------------------
httpx>=0.25.2,<1.0

# ---- Testing (dev only) -------------------------------------------------
pytest>=7.4.3
pytest-asyncio>=0.21.1
respx>=0.20.0
groq>=0.4.2
pydub>=0.25.1
torch>=2.1.0
torchaudio>=2.1.0
soundfile>=0.12.1

# ---- CI / Quality (dev only) --------------------------------------------
ruff>=0.4.0
pip-audit>=2.7.0
bandit>=1.7.0
python-json-logger>=2.0.0
